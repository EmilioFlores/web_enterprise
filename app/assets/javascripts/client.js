$(function(){

	var action = $('body').data().action;

	if (action == 'new' || action == 'edit') {
		// $('.spouse_items').nestedFields({
		// 	containerSelector: 'spouse_items',
		//     itemSelector: 'new_spouse_item'
		// });
	};

});